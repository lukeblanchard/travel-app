<div class="list-group">
    <br>
    <a href="/guides/{{this.id}}" class="list-group-item list-group-item-action flex-column align-items-start list-group-item-light" name="card">
        <div class="d-flex w-100 justify-content-between">
            <h3 class="mb-1" id="repoName">{{this.fname}} {{this.lname}}</h3>
            <small id="reposLang">{{this.city}}, {{this.country}}</small>
        </div>
    </a>
</div>
